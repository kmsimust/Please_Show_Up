const defaultConfig = {
  projectName: 'skinx-react-frontend',
  apiPath: 'http://localhost:3001/api',
  defaultConfig: {
    limit: 10,
  }
};

const config = {
  ...defaultConfig,
};

export default config;
